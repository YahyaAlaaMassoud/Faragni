import { StarRatingPipePipe } from './star-rating-pipe.pipe';

describe('StarRatingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StarRatingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
